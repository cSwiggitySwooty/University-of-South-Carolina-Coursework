package factorydesignpattern;

/**
 * Shape is an enum that is used for easily defining the shapes of cakes rather than using a string.
 * @author colin
 *
 */

public enum Shape {
	ROUND, SQUARE, SHEET, CUPCAKES, BUNT
}
